# Bot Telegram (con job-queue per reminder)
python-telegram-bot[job-queue]>=20.0

# Web scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Timezone support
pytz>=2023.3

# Environment variables (opzionale ma consigliato)
python-dotenv>=1.0.0

# ========================================
# AI & Database (NUOVI)
# ========================================

# HTTP async client per Ollama API
aiohttp>=3.9.0

# Supabase database (opzionale - solo se vuoi usare Supabase)
# Decommentare solo se configuri Supabase:
# supabase>=2.0.0
